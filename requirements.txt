Flask==3.1.3
# Pinned explicitly even though Flask depends on it: generate_password_hash() is called
# without method=, so the password hashing algorithm is whatever the resolved Werkzeug
# defaults to. Unpinned, a deploy could silently change it. 3.1.8 defaults to
# scrypt:32768:8:1. Re-check that default before bumping this.
Werkzeug==3.1.8
requests==2.32.5
gunicorn==25.3.0
psycopg2-binary==2.9.12
Flask-Limiter==4.1.1
redis==8.0.0
Authlib==1.3.2
firebase-admin==6.5.0
